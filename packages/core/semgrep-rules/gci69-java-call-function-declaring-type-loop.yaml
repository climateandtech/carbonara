rules:
  - id: gci69-java-call-function-declaring-type-loop
    languages:
      - java
    severity: INFO
    message: >-
      Do not call a function when declaring a for-type loop in order to avoid function calls each iterations. It saves CPU cycles.
    pattern-regex: 'for\s*\([^;]*;\s*[^;]*<\s*\w+\([^)]*\)\s*;[^)]*\)'
