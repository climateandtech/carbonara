rules:
  - id: gci72-python-executing-sql-queries-loop-induced
    languages:
      - python
    severity: INFO
    message: >-
      Executing SQL queries in loop induced unnecessary calculation by the CPU, RAM usage and network transfer.
    pattern: |
      for $ID in $RANGE:
          ...
          $CURSOR.execute($QUERY, ...)
          ...
